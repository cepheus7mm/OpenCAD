<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">
    
    <!-- Custom Window Style with Themed Titlebar -->
    <Style x:Key="ThemedWindowStyle" TargetType="{x:Type Window}">
        <Setter Property="shell:WindowChrome.WindowChrome">
            <Setter.Value>
                <shell:WindowChrome 
                    CaptionHeight="32"
                    ResizeBorderThickness="8"
                    CornerRadius="0"
                    GlassFrameThickness="0"
                    UseAeroCaptionButtons="False" />
            </Setter.Value>
        </Setter>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Window}">
                    <Border BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="{DynamicResource PrimaryBackgroundBrush}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="32" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            
                            <!-- Custom Title Bar -->
                            <Border Grid.Row="0" Background="{DynamicResource TitleBarBackgroundBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Window Icon (optional) -->
                                    <Border Grid.Column="0" Width="32" Height="32">
                                        <Image Source="{TemplateBinding Icon}" 
                                               Width="16" Height="16" 
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                    </Border>
                                    
                                    <!-- Window Title -->
                                    <TextBlock Grid.Column="1" 
                                              Text="{TemplateBinding Title}"
                                              Foreground="{DynamicResource TitleBarTextBrush}"
                                              FontSize="12"
                                              VerticalAlignment="Center"
                                              Margin="8,0,0,0" />
                                    
                                    <!-- Window Control Buttons -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                        <!-- Minimize Button -->
                                        <Button Name="PART_MinimizeButton" 
                                                Width="46" Height="32"
                                                shell:WindowChrome.IsHitTestVisibleInChrome="True"
                                                Style="{DynamicResource TitleBarButtonStyle}"
                                                ToolTip="Minimize">
                                            <Path Data="M 18,15 L 28,15" 
                                                  Stroke="{DynamicResource TitleBarTextBrush}" 
                                                  StrokeThickness="1"/>
                                        </Button>
                                        
                                        <!-- Maximize/Restore Button -->
                                        <Button Name="PART_MaximizeRestoreButton" 
                                                Width="46" Height="32"
                                                shell:WindowChrome.IsHitTestVisibleInChrome="True"
                                                Style="{DynamicResource TitleBarButtonStyle}"
                                                ToolTip="Maximize">
                                            <Path x:Name="PART_MaximizeRestorePath" 
                                                  Data="M 18,11 L 18,25 L 32,25 L 32,11 Z M 18,11 L 32,11" 
                                                  Stroke="{DynamicResource TitleBarTextBrush}" 
                                                  StrokeThickness="1" 
                                                  Fill="Transparent"/>
                                        </Button>
                                        
                                        <!-- Close Button -->
                                        <Button Name="PART_CloseButton" 
                                                Width="46" Height="32"
                                                shell:WindowChrome.IsHitTestVisibleInChrome="True"
                                                Style="{DynamicResource CloseButtonStyle}"
                                                ToolTip="Close">
                                            <Path Data="M 18,11 L 28,21 M 28,11 L 18,21" 
                                                  Stroke="{DynamicResource TitleBarTextBrush}" 
                                                  StrokeThickness="1"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            
                            <!-- Window Content -->
                            <ContentPresenter Grid.Row="1" />
                        </Grid>
                    </Border>
                    
                    <ControlTemplate.Triggers>
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter TargetName="PART_MaximizeRestorePath" Property="Data" 
                                    Value="M 18,13 L 18,24 L 29,24 L 29,13 Z M 20,11 L 20,13 L 31,13 L 31,22 L 29,22"/>
                            <Setter TargetName="PART_MaximizeRestoreButton" Property="ToolTip" Value="Restore"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Title Bar Button Styles -->
    <Style x:Key="TitleBarButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource TitleBarButtonHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource TitleBarButtonPressedBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource CloseButtonHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource CloseButtonPressedBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>